# Generated by Ship — install bundle
# Learn more: https://app.ship.elmundi.com
preset: monorepo
version: 2
shipctl_min: 0.12.0
repo: ElMundiUA/ship
api:
  base_url: "https://ship.elmundi.com"
  channel: stable
  ttl_hours: 24
  offline_ok: true
stack:
  tracker: none
  ci: gh-actions
  agents: []
  language: multi
agent:
  default: {}
  overrides: {}
process:
  id: development
  name: Development
  primary: true
  states:
  - id: planning
    name: Planning
    state: planning
    specialist:
      id: devops_platform
      name: DevOps/platform
      agent_profile: main
    instructions: Execute this state using the task context, specialist role, and runtime
      pattern discovery. Do not assume a static pattern mapping.
    triggers:
    - type: manual
      interval: null
      event: null
  - id: dev_implementation
    name: Implementation
    state: executing
    specialist:
      id: developer
      name: Developer
      agent_profile: main
    instructions: Execute this state using the task context, specialist role, and runtime
      pattern discovery. Do not assume a static pattern mapping.
    triggers:
    - type: manual
      interval: null
      event: null
  - id: validation
    name: Validation
    state: executing
    specialist:
      id: devops_platform
      name: DevOps/platform
      agent_profile: main
    instructions: Execute this state using the task context, specialist role, and runtime
      pattern discovery. Do not assume a static pattern mapping.
    triggers:
    - type: manual
      interval: null
      event: null
  - id: code_review
    name: Code Review
    state: reviewing
    specialist:
      id: code_reviewer
      name: Code reviewer
      agent_profile: main
    instructions: Execute this state using the task context, specialist role, and runtime
      pattern discovery. Do not assume a static pattern mapping.
    triggers:
    - type: manual
      interval: null
      event: null
  transitions:
  - from: planning
    to: dev_implementation
  - from: dev_implementation
    to: validation
  - from: validation
    to: code_review
  schedule:
    trigger:
      kind: schedule
      event: null
    time_zone: UTC
    slots:
    - id: slot_1700_2
      label: Tue 17:00
      local_time: '17:00'
      weekdays:
      - 2
      specialist_ids:
      - developer
  process_schema_version: 1
  routines: {}
