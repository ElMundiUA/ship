# `runtime/` — ship-agent package

Node **ship-agent** CLI (`dist/cli.js`), automation **`scripts/`**, and **`config/`**.

From the **repository root**: `npm install` (workspace), then `node runtime/dist/cli.js --help`.

`.env` is expected at the **repository root** (sibling of `runtime/`), not inside this folder.
