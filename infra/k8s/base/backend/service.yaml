apiVersion: v1
kind: Service
metadata:
  name: ship-server
  labels:
    app.kubernetes.io/name: ship-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ship-server
  ports:
    - name: http
      port: 80
      targetPort: 8100
