/**
 * Vitest setup — extends ``expect`` with jest-dom matchers and silences
 * the Next-link router error in components that mount a real <Link>
 * outside an App Router context.
 */

import "@testing-library/jest-dom/vitest";
