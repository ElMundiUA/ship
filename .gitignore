site/
.cursor/
.playwright-cli/
.playwright-mcp/
.venv-docs/
.venv/
node_modules/
apps/landing/.next/
apps/landing/.playwright-cli/
apps/landing/public/.book.tmp.html
apps/landing/public/.book.tmp.html.md
apps/landing/tsconfig.tsbuildinfo
apps/console/.next/
apps/console/tsconfig.tsbuildinfo
apps/console/.dockerignore.bak
output/playwright/
e2e/test-results/
test-results/
# Eval runner — captured artifacts + judge verdicts are per-run, not source.
# metrics.jsonl tracks score trends; commit it deliberately for baseline pinning.
tools/eval/runs/
tools/eval/results/
tools/eval/metrics.jsonl
.env
.env.*
!.env.example
!.env.shared
!e2e/.env.deployed.example
!e2e/.env.local.example

# K8s prod secrets — materialized locally from .env, never committed.
infra/k8s/overlays/*/secrets.env

.DS_Store
agent-run-*.log
.ship/agent-runs/
.ship/cache/
.ship/state/
.ship/telemetry-outbox.jsonl
.ship/feedback-drafts/
**/__pycache__/
**/.pytest_cache/
**/.ruff_cache/
**/.playwright-cli/
.claude/
apps/backend/.chroma/

# Local-only working trees: presentation decks, scratch outputs.
# Commit deliberately if you want any of these published.
presentations/
output/

# Внутренняя кухня — pricing, GTM, financials, рабочие заметки.
# Не публиковать в git: расчёты юнит-экономики, черновики стратегий, переписки.
_internal/

# Internal planning docs, closed-beta epics, prod-config checklists.
# Stay on disk for reference but never published. Was tracked historically;
# untracked via `git rm -r --cached` — files remain locally.
documentation/internal/*
# Ops runbooks (e.g. file-overlap detection ELS-154) are shared with implementers.
!documentation/internal/operations/
!documentation/internal/operations/**
