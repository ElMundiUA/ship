{
  "name": "ship-e2e-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Compact feedback API service — Ship pipeline e2e sandbox.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
